package com.hereliesaz.blusnu.ui

/**
 * Enum for filtering the device list by protocol.
 */
enum class FilterProtocol {
    ALL,     // Show all devices.
    CLASSIC, // Show only Bluetooth Classic (BR/EDR).
    BLE      // Show only Bluetooth Low Energy.
}
